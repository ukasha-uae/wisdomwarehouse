
'use server';
/**
 * AI Flow disabled as per user request.
 */
export async function aiPostDescriptionSuggestions(input: any): Promise<any> {
  return { description: "AI suggestions are currently disabled." };
}
